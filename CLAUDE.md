# Claude instructions

Read and follow [`AGENTS.md`](AGENTS.md) as the canonical agent contract, then read `docs/PRODUCT.md`, `docs/ARCHITECTURE.md` and `docs/ROADMAP.md` before architectural work.

Do not treat the historical `v1` branch as an implementation template for v2. Preserve the Lumic product identity/logo, not the Laravel architecture.
