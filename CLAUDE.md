# Claude instructions

Read and follow [`AGENTS.md`](AGENTS.md) as the canonical agent contract, then read `docs/PRODUCT.md`, `docs/ARCHITECTURE.md` and `docs/ROADMAP.md` before architectural work.

The full product name is **Lumic Control Center**; **Lumic** is the normal short name for prose, commands and technical identifiers. Do not treat the historical `v1` branch as an implementation template for v2. Preserve the Lumic Control Center product identity/logo, not the Laravel architecture.
